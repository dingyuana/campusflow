# 🛠️ Python 虚拟环境配置指南

## 什么是虚拟环境？

虚拟环境（Virtual Environment）是 Python 项目开发的**最佳实践**，它可以：
- ✅ 隔离项目依赖，避免与系统 Python 环境冲突
- ✅ 每个项目使用独立的依赖版本
- ✅ 方便团队协作，确保环境一致性

---

## 📋 快速开始

### 1. 创建虚拟环境

```bash
# 在项目根目录执行
python -m venv venv
```

这会创建一个 `venv` 文件夹，包含独立的 Python 解释器和依赖库。

### 2. 激活虚拟环境

**Windows:**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

**激活成功标志：** 命令行前显示 `(venv)`
```bash
(venv) user@computer:~/CampusFlow$
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 退出虚拟环境

```bash
deactivate
```

---

## 🔧 常用命令

| 命令 | 说明 |
|------|------|
| `python -m venv venv` | 创建虚拟环境 |
| `source venv/bin/activate` | 激活虚拟环境（Mac/Linux） |
| `venv\Scripts\activate` | 激活虚拟环境（Windows） |
| `deactivate` | 退出虚拟环境 |
| `pip list` | 查看已安装的包 |
| `pip freeze > requirements.txt` | 导出依赖列表 |

---

## ❗ 常见问题

### Q1: 提示 "No module named 'venv'"
```bash
# 安装 venv 模块
python3 -m pip install --user virtualenv
```

### Q2: 激活后没有 (venv) 标识
- 检查是否使用正确的激活命令（Windows/Mac 命令不同）
- 检查 venv 文件夹是否创建成功

### Q3: 安装依赖时报错
```bash
# 先升级 pip
pip install --upgrade pip

# 再安装依赖
pip install -r requirements.txt
```

### Q4: 如何删除虚拟环境？
```bash
# 直接删除 venv 文件夹
deactivate  # 先退出
rm -rf venv  # Mac/Linux
rmdir /s venv  # Windows
```

---

## ✅ 检查清单

在开始项目开发前，请确认：

- [ ] 已创建虚拟环境 `python -m venv venv`
- [ ] 已激活虚拟环境（看到 `(venv)` 标识）
- [ ] 已安装依赖 `pip install -r requirements.txt`
- [ ] 已配置 `.env` 文件
- [ ] 已测试数据库连接 `python db/connect.py`

---

## 📝 注意事项

1. **永远不要** 将 `venv` 文件夹提交到 Git（已在 `.gitignore` 中排除）
2. **每次开发前** 都要激活虚拟环境
3. **安装新包后** 记得更新 `requirements.txt`：`pip freeze > requirements.txt`
